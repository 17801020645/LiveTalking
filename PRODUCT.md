# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

管理员与终端用户同等重要。

- 管理员：在 GPU 机上运营数字人工作台——开户、绑定形象、演示连麦、启动 Avatar 生成、管理 TTS。
- 终端用户：登录后对已发布形象连麦、管理自己的资产、提交定制数字人订单。

无自助注册；账号由管理员创建。

## Product Purpose

在实时流式数字人引擎之上，提供可登录的双角色控制面：账号与角色、形象目录、定制订单、演示连麦、生成任务与 TTS 管理。成功标准是两类用户都能完成各自工作，同时口型同步、打断、多模型与音视频推流仍可用。

## Positioning

不是匿名 GPU 演示页的换皮。差异在于：同一套实时口型与推流引擎上，用登录、角色与订单把「运营控制面」和「终端连麦」分开，而邻近的口型引擎产品通常只提供匿名演示接口。

## Operating Context

- 入口：默认 WebRTC 打开 `/` 进入 `/app/` Vue 3 双角色工作台（未登录到登录页，登录后按角色进管理员壳或用户壳）；原站控制台走 `/index.html`。
- 本机启动：仓库根目录 `./start.sh`，默认 WebRTC + wav2lip，服务约在 `8010`。
- 管理员演示连麦走登录后的 `POST /api/v1/admin/offer`，说话仍走 `POST /human` 等驱动接口（须同一管理员）；用户连麦走 `/api/v1/me/offer`。原站 `/index.html` 仍可匿名 `POST /offer`。
- 口型与推流依赖本机 GPU 与已入库形象数据。TTS 可经 8010 代理到本机 Omni 服务。
- 评估时看真实连麦画面与打断，而不是静态截图 alone。

## Capabilities and Constraints

已确认能力：多数字人模型、声音克隆、说话打断、WebRTC/RTMP/虚拟摄像头、动作编排、多并发、自定义形象、前端 API；`/app/` 上的登录双角色、订单、形象目录、管理员演示连麦（Echo / Chat）、用户已发布形象连麦。

必须保留：引擎能力（口型同步、打断、多模型、推流）。前端壳可以大改，不要求永远保留原站 `/` 作为唯一控制面。

原 `web/` 目前仍可匿名打开完整旧控制台；这是现状，不是不可替换的产品承诺。

`/app/` 双角色工作台的无障碍目标为 WCAG 2.2 AA（壳层与关键表单基线）。原站 `web/` 与 WebRTC 连麦媒体控件不在该目标内。

未决：无定价/许可对外主张。

## Brand Commitments

产品名是 LiveTalking。仓库有标志 `assets/LiveTalking-logo.png`。未将配色、字体或视觉风格锁定为品牌承诺；前端可以换视觉语言。

## Evidence on Hand

- 引擎演示视频见 README（wav2lip / ernerf / musetalk 的 Bilibili 链接）。
- 标志：`assets/LiveTalking-logo.png`。
- 无客户案例、证言、定价或商用客户名单可引用。未来设计不得虚构客户、口碑或业绩数字。

## Product Principles

1. 管理端与用户端同等重要，任一端的任务完成度都不能为另一端让路。
2. 控制面服务引擎，不替换引擎：账号、订单、目录建立在实时口型与推流之上。
3. 口型、打断、多模型与推流是不变量；界面可以重做。
4. 只用仓库里真实存在的能力与素材，不编造客户与成果。
