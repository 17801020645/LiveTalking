---
generated_from_state_version: 7
---

# 验证

## 当前结果

- 结果: **验收通过，可归档**
- 验证情况: **已完成检查，验证结果已确认**
- 目标周期: 1
- 迭代: 1
- 验证器尝试次数: 1
- 完成时间: 2026-09-10T02:46:04.715Z
- 摘要: A1–A6 均由实现与 Runtime 检查覆盖：下单权限、订单隔离、接单驳回、交付绑定未发布、无视频拒绝生成、旧站与发布仍可用。

## 验收

| 编号 | 结果 | 来源 | 验收项 | 原因 |
| --- | --- | --- | --- | --- |
| A1 | passed | brief.md | A1：普通用户登录后可提交订单（视频或图片+音频+文字）；未登录或管理员提交用户下单接口返回 401/403 | 用户可 multipart 下单；管理员 403、未登录 401；图+音频+文字也可提交。 |
| A2 | passed | brief.md | A2：用户只能看到自己的订单；不能调用 `POST /api/avatar/task`（401 未登录或 403 非管理员） | 订单按 user_id 隔离；用户 POST /api/avatar/task 为 403，未登录 401。 |
| A3 | passed | brief.md | A3：管理员可接单或驳回；驳回后用户能看到原因 | 管理员可接单/驳回；驳回须填原因，用户列表可见 reject_reason。 |
| A4 | passed | brief.md | A4：管理员从已接单订单启动生成，任务成功后订单变为已交付，该用户资产中出现新形象且未自动发布 | 已接单订单生成后 fake 路径交付：资产出现且 is_published=0，首页无自动发布。 |
| A5 | passed | brief.md | A5：缺少视频的图+音频订单仍可提交并被接单；管理员若在无视频时点生成，得到明确错误而不是空跑 GPU | 图+音频可下单并接单；无视频生成返回 400 且提示需要视频，不进入 GPU。 |
| A6 | passed | brief.md | A6：原 `web/` 与用户资产/发布行为保持可用 | web/ 无差异；旧 avatar.html、/offer 与用户资产发布仍可用。 |

## 检查

| 检查 | 命令 | 工作目录 | 状态 | 退出码 | 耗时 |
| --- | --- | --- | --- | ---: | ---: |
| Platform and order unit tests | -m unittest tests.test_platform_v1 tests.test_orders | . | passed | 0 | 3854 ms |
| Original web/ unchanged | diff --exit-code -- web/ | . | passed | 0 | 13 ms |

## 阻塞项

_无。_

## 风险与跳过的工作

- 真实 GPU 交付依赖 TaskManager.on_status 回调，单测走 fake_avatar_tasks。
- 图+音频不自动变口型，无视频只能收单不能生成。

## 之前的迭代

| 目标周期 | 迭代 | 尝试 | 结果 | 未解决项 | 摘要 | 完成时间 |
| ---: | ---: | ---: | --- | --- | --- | --- |
| 1 | 1 | 1 | pass | — | A1–A6 均由实现与 Runtime 检查覆盖：下单权限、订单隔离、接单驳回、交付绑定未发布、无视频拒绝生成、旧站与发布仍可用。 | 2026-09-10T02:46:04.715Z |



## 结论

A1–A6 均由实现与 Runtime 检查覆盖：下单权限、订单隔离、接单驳回、交付绑定未发布、无视频拒绝生成、旧站与发布仍可用。
